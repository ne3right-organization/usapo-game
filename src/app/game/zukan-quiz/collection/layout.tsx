import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "市区町村図鑑クイズ | 図鑑",
};

export default function ZukanCollectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
