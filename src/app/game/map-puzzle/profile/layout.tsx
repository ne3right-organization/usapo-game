import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "地図パズル | マイプロフィール",
};

export default function ProfileLayout({ children }: { children: React.ReactNode }) {
  return children;
}
