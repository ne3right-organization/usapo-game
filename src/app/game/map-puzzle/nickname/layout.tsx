import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "地図パズル | ニックネーム設定",
};

export default function NicknameLayout({ children }: { children: React.ReactNode }) {
  return children;
}
