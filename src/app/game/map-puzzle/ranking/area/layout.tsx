import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "地図パズル | エリアランキング",
};

export default function AreaRankingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
